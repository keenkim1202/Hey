# 변경 기록

[English](CHANGELOG.md) · 한국어

Claude Code 는 브랜치에서 설치하므로 모든 커밋이 그대로 갑니다. 무엇이 바뀌었는지는 이 파일로
확인합니다. Codex 는 `.codex-plugin/plugin.json` 의 버전을 고정하므로, 여기 적힌 릴리스가 곧
Codex 사용자가 실제로 받는 것입니다.

## 미출시

Claude Code 는 이미 받았습니다. Codex 는 고정 버전을 다음에 올릴 때 받습니다.

### 새로 들어온 것

- **base 브랜치는 ref 이고, `origin/` 은 그것을 찾아볼 장소 중 하나입니다.** 모든 비교가 원격
  사본을 이름에 박아 넣고 있어서, 원격이 없는 저장소는 base 를 아예 해석할 수 없었습니다.
  그런 저장소에도 작업이 올라가는 브랜치가 있고, 아직 거기 도달하지 않은 커밋이 쌓이는데도요.
  둘 다 있으면 원격 사본을 쓰고, 로컬 브랜치뿐이면 그것을 씁니다. `dirty` 는 그 숫자를 위험한
  작업이 아니라 `not yet in main` 으로 보고합니다 — 푸시할 곳이 없으면 "미푸시" 는 저장소의 모든
  커밋이고 결코 줄지 않기 때문입니다.
- **`add` 가 저장소 아닌 디렉터리 아래에서 저장소를 찾으면 알려 줍니다.** 코드가
  `Project/Sources/` 에 있는데 `Project/` 를 등록하는 건 흔한 실수입니다. 경로를 출력하고
  거기서 멈춥니다 — 프로젝트 하나는 저장소 하나이고, 아무도 지목하지 않은 저장소를 끌어다 쓰면
  그 커밋이 한 번도 담은 적 없는 프로젝트 아래로 들어갑니다. 두 단계까지만 보고, 점으로 시작하는
  디렉터리와 벤더 트리는 건너뜁니다.

### 짚어 둘 수정

- **원격이 없는 저장소가 더 이상 `doctor` 에서 실패하지 않습니다.** `origin/<base> 가 없다`
  고 말하며 base 브랜치를 지정해 다시 등록하라고 했는데, base 가 올라앉을 원격 자체가 없으니
  도움이 되지 않는 안내였습니다. 그 검사를 지나는 모든 경로가 실패했고 어느 쪽에도 조치가
  없었습니다. 이제는 사실을 말하는 경고입니다 — 작업이 이 기계를 떠날 수 없으므로 미푸시 커밋은
  여기서 척도가 아니며, 커밋·코드량·워크트리는 그대로 동작합니다. 진짜로 잘못 설정된 base —
  원격은 있는데 `origin/<base>` 가 없는 경우 — 는 여전히 실패하고, 고치는 방법도 그대로
  알려 줍니다.
- **`dirty` 가 "잴 것이 없다" 와 "못 쟀다" 를 구분합니다.** 저장소가 아닌 디렉터리, 푸시할
  곳이 없는 저장소, 정말로 base 가 해결되지 않은 경우 — 세 상태가 한 문장을 출력했습니다.
  오설정은 마지막 하나뿐이고, 이제 마지막 하나만 무언가를 설정하라고 요구합니다.
- **`add` 가 없는 것을 말하지 않습니다.** git 저장소가 아닌 디렉터리를 등록하면
  `base: unresolved — --base <브랜치> 로 다시 실행하라` 고 했고, 존재하지도 않는
  `.git/info/exclude` 에 넣으라고 했습니다. 이 도구의 체크리스트 절반은 원래 git 이 필요 없고,
  등록 시점에 그렇게 말합니다.

## 0.2.0

0.1.0 이후 71커밋이 쌓였고 Codex 는 그중 하나도 못 받았습니다 — 고정된 버전을 한 번도 안
올렸기 때문입니다. 이번에 올렸고, 셀프테스트의 검사가 재발을 막습니다.

**0.1.0 을 쓰고 계셨다면 갱신 전에 알아 둘 동작 변경이 넷 있습니다.**

### 동작 변경

- **블로커는 이제 단어가 아니라 마커입니다.** 항목 본문에 "API 대기" 라고 써도 더 이상 분류되지
  않습니다. `[blocked]` 를 붙이거나 블로커 제목 아래 두세요. 평범한 문장이 그 단어들을 쓰는데,
  실수로 후보에서 빠진 항목은 아무도 다시 권해 주지 않습니다. `doctor` 가 대기처럼 읽히는데
  마커가 없는 줄을 짚어 주므로, 예전 방식으로 쓴 원장이 조용해지지는 않습니다.
- **`snapshot --date` 가 과거에 체크박스 상태를 쓰지 않습니다.** `collect --date` 는 원래
  거부하던 것입니다. 원장은 체크박스의 이력을 갖지 않으므로, 오늘 상태를 과거 날짜로 찍으면
  기준선이 옮겨지고 이후 모든 날이 0 으로 읽혔습니다.
- **`note` 가 `--scope` 를 받지 않습니다.** 메모는 원장 하나에만 들어갑니다. 그 플래그는
  파싱되고, 아무것도 안 바꾸고, 안 바꿨다는 말도 안 했습니다.
- **순위가 전부 사라졌습니다.** 보드·스트릭·주간 페이스·개인 최고·일일 목표가 없습니다. 작업량은
  도구 자신이 검증할 수 없는 산정치 위에 서 있고, 코드량·토큰량은 성과가 아니라 활동량입니다 —
  그 숫자들은 전부 장부상의 선택을 성과처럼 입혀 놓은 것이었습니다.

### 원장을 읽는 방식이 달라졌습니다

- **마커.** `[id <이름>]` 이 항목의 정체를 고정해서, 이름을 바꿔도 이력이 끊기지 않습니다 —
  모든 항목에 붙일 값이 있습니다. 안 붙이면 리네임이 이미 닫힌 칸을 한 번 더 적립합니다.
  `[blocked]`, `[since YYYY-MM-DD]`, `[branch <이름>]`, 하위 항목의 `[AI n]` 도 산문이 아니라
  데이터로 읽힙니다.
- **코드 펜스 안은 건너뜁니다.** 원장 안에 인용한 체크리스트 줄이 작업으로 집계되고 있었습니다.
- **템플릿 자리표시자도 건너뜁니다.** 갓 만든 원장이 자기 예시를 항목·하위 항목·블로커로
  세지 않습니다.
- **쌓이기만 하는 절은 두 번째 파일에 둘 수 있습니다** — `add <root> --ledger-log <경로>` 가
  작업 로그와 메모를 그쪽으로 보내고 체크리스트는 원래 자리에 남깁니다.

### 새로 생긴 것

- `draft-log` — git 이력에서 작업 로그 초안을 뽑습니다. 오늘 만든 원장에 몇 달 된 저장소를
  붙일 때 씁니다. 출력만 하고 아무것도 쓰지 않습니다.
- `import-tasks` — spec-kit 의 `tasks.md` 를 원장 항목으로 바꿉니다. `T001` 이 `[id t001]` 이
  됩니다. 산정치는 지어내지 않습니다.
- `variance` — 기록된 날의 닫힌 AI 를 그날 커밋 구간과 나란히 놓습니다. 여기 있는 유일한 교정
  수치입니다. 하한이며, 근무 시간 기록이 아닙니다.
- `pr-sync` — 머지된 PR 이 며칠 열려 있었는지 말하고, PR 이 가리키는데 아직 안 닫힌 항목을
  모아 줍니다. 제안할 뿐 체크하지 않습니다.
- 토큰 비용 — `config.json` 의 `token_cost` 에 넣은 단가로 계산합니다. 내장 단가표는 없습니다.
  여기 박아 두면 단가가 바뀌는 주에 틀립니다. 단가가 없으면 비용 줄도 없습니다.
- `/next` — 다음 한 항목만 브리핑하고 시작할지 묻습니다.
- `HEY_WIDTH` — 넓은 터미널에서 카드를 펼칩니다.

### 짚어 둘 수정

- 원장과 `config.json` 을 원자적으로 씁니다. 둘 다 쓰기가 중단되면 잘려 나갈 수 있었고, 원장은
  커밋되지 않으므로 되돌릴 사본이 없었습니다.
- 미푸시 판정을 원격 기준으로 해서, 스쿼시 머지된 브랜치를 "잃기 직전의 작업" 으로 보고하지
  않습니다.
- 토큰을 경로로 프로젝트에 귀속시킵니다. `alpha-ios` 가 `alpha` 로 흘러들지 않습니다.
- 하루의 마지막 1분에 난 커밋이 어느 날에도 안 세어지던 것을 고쳤습니다.
- 읽고 쓰는 모든 파일에 `encoding="utf-8"` 을 지정했습니다. 한글 원장이 Windows 레거시
  코드페이지에서 `UnicodeDecodeError` 를 냈습니다.
- 첫 기록이 카드에서 `기준선` 으로 읽힙니다. `collect` 가 원래 하던 말과 같아졌습니다 —
  전에는 `0.00 AI-일` 이었습니다.

## 0.1.0

첫 릴리스. Codex 지원, 스탠드업 스킬, 항목별 이력.
