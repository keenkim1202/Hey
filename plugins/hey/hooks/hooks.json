{
  "description": "hey — at session start, report only work that is uncommitted, or committed where no remote has it",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/on-session-start.py\"",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
